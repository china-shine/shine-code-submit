{
  "name": "tokenserver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "接收 shine-code-submit 报表上报,三级展示(用户/项目/token详情)",
  "scripts": {
    "dev": "bun run src/main.ts",
    "start": "bun run src/main.ts",
    "build:css": "bunx @tailwindcss/cli -i ui/styles/index.css -o ui/.build/style.css --minify",
    "build:ui": "bun run scripts/build-ui.ts",
    "build": "bun run scripts/build.ts"
  },
  "dependencies": {
    "date-fns": "^4.4.0",
    "lucide-react": "^0.487.0",
    "react": "^19.2.7",
    "react-day-picker": "^10.0.1",
    "react-dom": "^19.2.7",
    "recharts": "^2.15.2"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "tailwindcss": "^4.1.12"
  }
}
